import { SafeAreaView } from 'react-native-safe-area-context';
import Login from './modules/login';

const App = () => {
  return (
    <SafeAreaView>
      <Login />
    </SafeAreaView>
  );
};

export default App;
